
# LeetCode No.004 寻找两个正序数组的中位数

## 题目链接: https://leetcode.cn/problems/median-of-two-sorted-arrays/description/

## 题目描述
给定两个大小分别为 m 和 n 的正序（从小到大）数组 nums1 和 nums2。请你找出并返回这两个正序数组的 中位数 。

算法的时间复杂度应该为 O(log (m+n)) 
## 解题思路
- ** 方法 1 ** ：二分法
二分法的思路是通过二分法来找到两个数组的中位数。首先要保证nums1的长度小于等于nums2的长度，这样就可以保证在nums1中进行二分查找。j通过i来计算出来，j = (m + n + 1) // 2 - i。然后通过比较nums1[i-1]和nums2[j-1]来判断i的取值范围。如果nums1[i-1] > nums2[j-1]，说明i太大了，需要减小i；如果nums1[i-1] < nums2[j-1]，说明i太小了，需要增大i；如果nums1[i-1] == nums2[j-1]，说明找到了中位数。最后根据m+n的奇偶性来返回中位数。

时间复杂度`O(log(min(m,n)))`  
空间复杂度：`O(1)`

📌 [查看 Python 代码](../solutions/python/No_004_寻找两个正序数组的中位数.py)