# LeetCode No.53 最大子数组和

## 题目链接: https://leetcode.cn/problems/maximum-subarray/description/

## 题目描述
给你一个整数数组 nums ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。

子数组是数组中的一个连续部分。

## 解题思路
- ** 方法 1 ** ：贪心
因为要寻找最大的子数组和，所以我们想到用贪心。也就是每次都找局部的最大，那么我们可以有一个目前的数组和，如果和小于0，那么意味着此时前面的数字是累赘，我们应该从下面开始，如果和大于0说明利于我们找到更大的，所以我们可以保留，然后每次都更新result。

时间复杂度`O(N)` 
空间复杂度：`O(1)`

- ** 方法 2 ** ：动态规划
动态规划五部曲：
1. 确定dp数组以及下标的含义：
dp[i]的定义为：到下标i包括下标i位置之前的连续子数组的最大和
2. 确定递推公式：
dp[i] = max(dp[i-1]+nums[i],nums[i])
有两种状态，
dp[i - 1] + nums[i]，即：nums[i]加入当前连续子序列和
nums[i]，即：从头开始计算当前连续子序列和
3. dp数组如何初始化：
dp[0] = nums[0]因为第一个是nums[0]
4. 确定遍历顺序：
从递推公式可以看出来,是从前向后遍历。
5. 举例推导dp数组:
时间复杂度`O(N)` 
空间复杂度：`O(N)`

- ** 方法 3 ** ：动态规划+状态压缩
方法二中我们可以看到，dp[i]的值只和dp[i-1]有关，所以我们可以用一个变量来记录当前的连续子数组和，这样就可以将空间复杂度优化到O(1)。



📌 [查看 Python 代码](../solutions/python/No_053_最大子数组和.py)