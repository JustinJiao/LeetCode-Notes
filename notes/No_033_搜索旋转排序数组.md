# LeetCode No.33 搜索旋转排序数组

## 题目链接: https://leetcode.cn/problems/search-in-rotated-sorted-array/description/

## 题目描述
整数数组 nums 按升序排列，数组中的值 互不相同 。

在传递给函数之前，nums 在预先未知的某个下标 k（0 <= k < nums.length）上进行了 向左旋转，使数组变为 [nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]]（下标 从 0 开始 计数）。例如， [0,1,2,4,5,6,7] 下标 3 上向左旋转后可能变为 [4,5,6,7,0,1,2] 。

给你 旋转后 的数组 nums 和一个整数 target ，如果 nums 中存在这个目标值 target ，则返回它的下标，否则返回 -1 。

你必须设计一个时间复杂度为 O(log n) 的算法解决此问题。

## 解题思路
- ** 方法 1 ** ：二分查找
由于数组是旋转排序数组，我们可以利用二分查找来找到目标值。具体步骤如下：
1. 初始化两个指针left和right，分别指向数组的开头和结尾。
2. 当left <= right时，计算中间位置mid = left + (right - left) // 2。
3. 如果nums[mid]等于目标值target，直接返回mid。
4. 如果nums[mid] >= nums[left]，说明左半部分是有序的：
   - 如果目标值target在左半部分范围内（nums[left] <= target < nums[mid]），更新right = mid - 1。
   - 否则，更新left = mid + 1。
5. 否则，说明右半部分是有序的：
   - 如果目标值target在右半部分范围内（nums[mid] < target <= nums[right]），更新left = mid + 1。
   - 否则，更新right = mid - 1。
6. 最后，如果没有找到目标值，返回 -1。
时间复杂度`O(log n)`  
空间复杂度：`O(1)`


📌 [查看 Python 代码](../solutions/python/No_033_搜索旋转排序数组.py)