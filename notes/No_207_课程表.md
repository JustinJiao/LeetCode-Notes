# LeetCode No.207 课程表

## 题目链接: https://leetcode.cn/problems/course-schedule/description/

## 题目描述
给你一个整数 numCourses ，表示总课程数，课程编号从 0 到 numCourses-1 。同时给你一个数组 prerequisites ，其中 prerequisites[i] = [ai, bi] ，表示在选修课程 ai 前 必须 先选修课程 bi 。

请你判断是否可能完成所有课程的学习？如果可以，返回 true ；否则，返回 false 。

## 解题思路
- ** 方法 1 ** ：拓扑排序（Kahn 算法）
使用拓扑排序算法（Kahn 算法）来判断是否存在环。首先构建图的邻接表表示，并计算每个节点的入度。然后使用队列存储所有入度为 0 的节点，依次处理这些节点，并将其邻接节点的入度减 1。如果邻接节点的入度变为 0，则将其加入队列。最终，如果处理的节点数量等于 numCourses，则返回 true；否则返回 false，表示存在环。
时间复杂度：`O(V + E)`，其中 V 是课程数，E 是先修课程关系数。   
空间复杂度：`O(V + E)`，用于存储邻接表和入度数组。

📌 [查看 Python 代码](../solutions/python/No_207_课程表.py)