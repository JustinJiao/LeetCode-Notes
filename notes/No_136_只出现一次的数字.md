# LeetCode No.136 只出现一次的数字

## 题目链接: https://leetcode.cn/problems/single-number/description/

## 题目描述
给你一个 非空 整数数组 nums ，除了某个元素只出现一次以外，其余每个元素均出现两次。找出那个只出现了一次的元素。

你必须设计并实现线性时间复杂度的算法来解决此问题，且该算法只使用常量额外空间。
## 解题思路
- ** 方法 1 ** ：使用异或运算
异或运算有以下几个性质：
1. 任何数和0做异或运算，结果仍然是原来的数，即 a⊕0=a
2. 任何数和其自身做异或运算，结果是0，即 a⊕a=0
3. 异或运算满足交换律和结合律，即 a⊕b=b⊕a 和 (a⊕b)⊕c=a⊕(b⊕c)
根据以上性质，我们可以将数组中的所有元素进行异或运算。由于每个元素出现两次，异或运算会将它们抵消掉，最终剩下的就是那个只出现一次的元素。
时间复杂度`O(N)` 因为我们需要遍历整个数组一次。  
空间复杂度：`O(1)` 因为我们只使用了一个变量来存储结果。

📌 [查看 Python 代码](../solutions/python/No_136_只出现一次的数字.py)